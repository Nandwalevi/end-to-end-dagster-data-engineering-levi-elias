version: "3.8"
services:
  dagster:
    build: .
    env_file: .env                 # loads SUPABASE_* vars
    ports:
      - "3000:3000"
    command: >
      dagster dev -h 0.0.0.0 -p 3000 -m dagster_project