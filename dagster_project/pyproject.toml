[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dagster_e2e"
version = "0.1.0"
dependencies = ["dagster", "pandas"]

[tool.pytest.ini_options]
testpaths = ["dagster_project/tests"]