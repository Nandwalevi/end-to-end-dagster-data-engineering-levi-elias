.env
venv
# ── Python / Dagster / dbt ──
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
.pytest_cache/
.coverage
*.cover
*.log
.tox/
.nox/
.hypothesis/
.dagster/
dagster_home/
dagster.yaml
dbt_packages/
logs/
target/
.dbt/

# ── Virtual environments (whatever name you used) ──
venv/
env/
ENV/
.venv/

# ── Jupyter / IPython ──
.ipynb_checkpoints
*.ipynb

# ── VS Code / IDE ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── Terraform ──
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
terraform.tfvars
override.tf

# ── Kubernetes / Kustomize ──
k8s/overlays/*/generated-*

# ── Docker ──
.dockerignore

# ── OS ──
.DS_Store
Thumbs.db

# ── Secrets / local config ──
.env
.env.*
!.env.example
service-account*.json
*kubeconfig*
*.pem
*.key
secrets.yml

# Terraform plan output
*.tfplan

# Kubernetes *generated* patches (if you produce them locally)
k8s/overlays/*/generated-*.yaml

# Temporary Helm packaging
*.tgz

# Local Minikube or Kind kubeconfig you downloaded
kubeconfig-*

# Cloud service-account keys
service-account-*.json
service-account-*.yaml